<br/><br/>

<dl class="dl-horizontal">
  <dt>Nombre:</dt>
  <dd><%= @category.name %></dd>
</dl>

<div class="form-actions">
  <%= link_to 'Volver', categories_path, :class => 'btn'  %>
  <%= link_to 'Editar', edit_category_path(@category), :class => 'btn' %>
  <%= link_to 'Eliminar', category_path(@category), :method => 'delete', :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => '¿Confirma eliminar?')), :class => 'btn btn-danger' %>
</div>
